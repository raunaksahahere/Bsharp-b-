note Function test

define function add with a and b do
    return a plus b
end

let result be call add with 3 and 7
say result

define function factorial with n do
    if n is less than 2 then
        return 1
    end
    return n times call factorial with n minus 1
end

say call factorial with 5